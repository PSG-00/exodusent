services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: postgres
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/${POSTGRES_DB:-exodusent}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-exodusent}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-exodusent_dev}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-exodusent}
      POSTGRES_USER: ${POSTGRES_USER:-exodusent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-exodusent_dev}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_data:
    name: exodusent_postgres_data
