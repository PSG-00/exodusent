# 작업 규칙

이 규칙은 저장소 전체에 적용한다.

## 변경 범위

- 사용자와의 설명은 한국어로 작성한다.
- 작업 전 관련 코드와 Git 상태를 확인하고 기존 사용자 변경을 보존한다.
- 요청 범위에 필요한 변경만 수행한다. 관련 없는 리팩터링이나 의존성 변경은 피한다.
- 버전·설정·API의 현재 상태는 실제 코드에서 확인한다. 이 문서에 구조 목록이나 작업 이력을 누적하지 않는다.

## 구현

- Controller는 HTTP 요청·응답, Service는 비즈니스 로직·트랜잭션, Repository는 데이터 접근을 담당한다.
- 생성자 주입을 사용하고 요청·응답 DTO는 기존 record 형식을 따른다. 엔티티를 API 응답으로 직접 반환하지 않는다.
- 엔티티 변경은 도메인 메서드로 표현한다. 조회 트랜잭션은 읽기 전용으로, 쓰기 작업은 쓰기 트랜잭션으로 구분한다.
- 도메인 오류는 `ErrorCode`와 `BusinessException`으로 표현하고 `GlobalExceptionHandler`에서 응답으로 변환한다.
- API 경로·상태 코드·오류 코드·응답 필드를 변경할 때 기존 클라이언트에 미치는 영향을 확인한다.
- 입력 검증은 실제 호출 경로까지 확인한다. DB 제약이나 예외 핸들러의 존재만으로 검증을 보장한다고 가정하지 않는다.

## 검증

- Gradle Wrapper를 사용한다. Windows 테스트 명령은 `.\gradlew.bat test`이며 Linux/macOS에서는 `./gradlew test`를 사용한다.
- 동작 변경은 관련 성공·실패 경로를 검증한다. 컨텍스트 로딩 테스트만으로 API 동작을 검증했다고 판단하지 않는다.
- 문서만 변경하면 내용과 형식을 확인한다. 실행하지 않은 테스트를 통과했다고 보고하지 않는다.
- 완료 시 변경 내용, 수행한 검증, 남은 제한 사항을 간결하게 전달한다.
